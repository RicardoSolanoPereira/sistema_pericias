import streamlit as st


def render(owner_user_id: int):
    st.header("🧾 Andamentos")
    st.info(
        "Tela de andamentos ainda não implementada. Próximo passo: histórico por processo (CRUD)."
    )
